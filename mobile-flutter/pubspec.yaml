name: temp_monitor_app
description: Temperature monitoring app
publish_to: 'none'
version: 1.0.0+1

environment: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mqtt_client: ^9.6.8
  charts_flutter: ^0.12.0
  firebase_core: ^2.24.0
  firebase_messaging: ^14.9.4
  flutter_local_notifications: ^13.0.0
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
